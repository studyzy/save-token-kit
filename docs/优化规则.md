1. Skill如果是通过/命令来触发的，而不是由AI自动发现触发的，则可以加上

```
disable-model-invocation: true
```

2. 列出各个Command，Skill和Agent的列表，并根据每个的Description，由AI判断需要LLM的逻辑的难度，如果是任务简单的，建议用户为Agent和SKILLS，Commands添加模型指定。

### Tools细化

3. 扫描各个子Agent的定义，看看是否有Tools定义，如果没有，则调用LLM给出Tools推荐，而不推荐的Tools就改为Defer的。
   参考： https://www.codebuddy.cn/docs/cli/tool-defer-overlay

4. MCP 参考`.mcp.json` 对于MCP，建议开启 "defer_loading": true
5. 强烈建议安装RTK，
   brew install rtk
   或者 curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh
6. 强烈建议安装Caveman
7. 强烈建议安装Ponytail
8. 询问用户是否安装代码知识库，如果安装，提供选项：GitNexus，CodeGraph，Codebase-memory-mcp，Graphify 选一个。
9. 建议用户安装Headroom。
10. 询问是否安装token-optimizer
